function estvalide (table){
    return table.innerHTML.length;
}
function rechercherVainqueur(pions, joueurs, tours) {
    pions[0].innerHTML === joueurs [tour]
}
if (A1 === "X" && A2 === "X" && A3 === "X"){
    console.log("X a gagné");
}
